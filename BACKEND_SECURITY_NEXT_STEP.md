# طرف الخيط — الخطوة الأمنية التالية

النسخة الحالية تصلح للتجربة والبيع اليدوي، لكن فيه حاجتين لا يمكن تأمينهما من HTML/JavaScript فقط:

## 1) Premium A / Premium B والأكواد

القضايا نفسها بقى عليها `premiumTier: 'A'` أو `premiumTier: 'B'`، لكن دالة Supabase الحالية `redeem_code(p_case_id, p_code)` ترجع نجاح/فشل فقط، والأكواد الحالية عامة لأي قضية.

عشان باقة مثل `2 Premium A + 4 Premium B` تبقى مُلزمة فعليًا، Supabase لازم يعرف نوع الرصيد أو نوع الكود. أنسب تصميم لاحقًا:

- كود باقة واحد بدل 6 أو 12 كود.
- سجل الباقة يحتوي `credits_a` و `credits_b`.
- عند فتح قضية، الـRPC يقرأ `premiumTier` المطلوب من السيرفر، يخصم من الرصيد الصحيح، ويسجل `case_id` كـ entitlement للعميل/الجهاز.
- لا يتم الخصم إذا القضية مفتوحة بالفعل.

## 2) حماية محتوى القضية المدفوعة

طالما ملفات `cases/*.js` الخاصة بالقضايا Premium تتحمل مع الصفحة، فـ `correctSuspectId` والنهايات والأدلة موجودة عند العميل حتى لو الواجهة مقفولة.

الحل الحقيقي:

- `play.html` يحمل Manifest عام فقط: العنوان، الغلاف، السعر، الفئة.
- بعد التحقق من entitlement في Supabase، يتم جلب بيانات القضية المدفوعة من endpoint/RPC أو Storage خاص.
- نتيجة الاتهام المهمة تتأكد على السيرفر لو الجائزة/الليدربورد لها قيمة مالية.

## 3) الليدربورد

تم إصلاح خطأين في النسخة الحالية: وقت البداية أصبح محفوظًا عند الـrefresh، والليدربورد العام يرسل `game.score` بدل `investigationPoints`.

لكن ما زالت النتيجة آتية من المتصفح؛ لو الليدربورد سيمنح جوائز مالية، لا تعتبر Client-side score مصدر ثقة نهائي. الأفضل أن يكون هناك session/run على السيرفر وتسجيل للأحداث أو تحقق server-side قبل اعتماد النتيجة.
